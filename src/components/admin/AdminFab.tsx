/**
 * @deprecated FAB flutuante foi removido em favor de uma ação contextual no
 * AdminMobileHeader (padrão de apps modernos: Gmail, Linear, Notion).
 * Mantido como componente vazio para não quebrar AdminLayout.
 */
export function AdminFab() {
  return null;
}
